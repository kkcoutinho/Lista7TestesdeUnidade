let areaQuadrado = (lado1, lado2) => lado1 * lado2
let areaParalelograma = (base, altura) => base * altura
const areaTriangulo = (base, altura) => (base * altura) / 2;

module.exports = {
    areaQuadrado,
    areaParalelograma,
    areaTriangulo
}